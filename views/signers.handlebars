<form method="POST">
    <h2>SIGNERS LIST 📝</h2>
    <ul>
        {{#signers}}
            <p><a href="{{url}}">{{first}}
                    {{last}}</a>
                {{age}}
                <a href="/signers/{{city}}">{{city}}</a>
            </p>
        {{/signers}}

    </ul>
</form>